<Window x:Class="_12A_Projektmunka.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_12A_Projektmunka"
        mc:Ignorable="d"
        Title="MainWindow" Height="1080" Width="1920" WindowState="Maximized" WindowStyle="None" FontSize="30">
    <Window.Background>
        <ImageBrush Opacity="0.5" ImageSource="/12A_Projektmunka;component/img/mainbackground.jpg"/>
    </Window.Background>
    <Window.Resources>
        <SolidColorBrush x:Key="TransparentBlack" Color="Black" Opacity="0.5"/>
        <SolidColorBrush x:Key="TransparentGray" Color="DarkGray" Opacity="0.5"/>
        <SolidColorBrush x:Key="CaretBrush" Color="White"/>
        <LinearGradientBrush x:Key="GrayToLightGrayGradient" Opacity="0.2" StartPoint="0,1" EndPoint="0,0">
            <GradientStop Offset="0" Color="LightGray"/>
            <GradientStop Offset="0.99" Color="Transparent"/>
            <GradientStop Offset="1" Color="Transparent"/>
        </LinearGradientBrush>
        <Style x:Key="RepeatButtonTransparent" TargetType="{x:Type RepeatButton}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="Background" Value="LightGray"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Border Background="{TemplateBinding Background}" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}" CornerRadius="10"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollBarThumbVertical" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <!--<Setter Property="Margin" Value="3"/>-->

            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="rectangle" Background="Gray" Height="{TemplateBinding Height}" SnapsToDevicePixels="True" Width="{TemplateBinding Width}" CornerRadius="8"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="rectangle" Value="Gray"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="true">
                                <Setter Property="Background" TargetName="rectangle" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="white"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="{StaticResource CaretBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="tbxBorder" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource TransparentGray}"/>
            <Setter Property="CornerRadius" Value="10"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!--gombok-->
        <Border Grid.Column="0" VerticalAlignment="Center" Background="{StaticResource TransparentBlack}" CornerRadius="50" Width="300">
            <Grid>
                <StackPanel>
                    <Button x:Name="weaponBtn" Content="Weapons" Margin="10" Height="70" Width="200"/>
                    <Button x:Name="aboutBtn" Content="About us" Margin="10" Height="70" Width="200"/>
                    <Button x:Name="quitBtn" Content="Quit" Margin="10" Height="70" Width="200"/>
                </StackPanel>
            </Grid>
        </Border>

        <!--lista-->
        <StackPanel Grid.Column="1">
            <Border Background="{StaticResource TransparentBlack}" CornerRadius="20,20,0,0" Width="550" Margin="0,10,0,0">
                <ComboBox x:Name="weaponTypeCbx"
          Width="500"
          Margin="0,10,0,0"
          Background="Transparent"
          ItemsSource="{Binding types}"
          SelectionChanged="weaponTypeCbx_SelectionChanged">
                    <ComboBox.Style>
                        <Style TargetType="ComboBox">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ComboBox">
                                        <Grid>
                                            <ToggleButton
                                Name="ToggleButton"
                                Content="ComboBox"
                                Focusable="false"
                                IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                ClickMode="Press"
                                Background="{TemplateBinding Background}"
                            />
                                            <Popup
                                Name="Popup"
                                Placement="Bottom"
                                IsOpen="{TemplateBinding IsDropDownOpen}"
                                AllowsTransparency="True"
                                Focusable="False"
                                PopupAnimation="Slide"
                            >
                                                <Grid
                                    Name="DropDown"
                                    SnapsToDevicePixels="True"
                                    MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    Background="Transparent"
                                >
                                                    <Border x:Name="DropDownBorder" Background="White" BorderThickness="1" BorderBrush="Black">
                                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                                        </ScrollViewer>
                                                    </Border>
                                                </Grid>
                                            </Popup>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ComboBox.Style>
                </ComboBox>
            </Border>
            <Border 
                Background="{StaticResource TransparentBlack}"
                CornerRadius="50"
                Padding="30">
                <ListBox x:Name="weaponLbx"
                             Height="850"
                             Width="560"
                             Margin="10"
                             ItemsSource="{Binding filteredWeapons}">
                    <ListBox.Resources>
                        <Style TargetType="{x:Type ScrollBar}">
                            <Setter Property="Background" Value="#505050"/>
                            <Setter Property="BorderBrush" Value="#505050"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                                        <Grid x:Name="Bg" SnapsToDevicePixels="true">
                                            <Grid.RowDefinitions>
                                                <RowDefinition MaxHeight="{DynamicResource {x:Static SystemParameters.VerticalScrollBarButtonHeightKey}}"/>
                                                <RowDefinition Height="0.00001*"/>
                                                <RowDefinition MaxHeight="{DynamicResource {x:Static SystemParameters.VerticalScrollBarButtonHeightKey}}"/>
                                            </Grid.RowDefinitions>
                                            <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Grid.Row="1" CornerRadius="10"/>
                                            <!--<RepeatButton x:Name="PART_LineUpButton" Command="{x:Static ScrollBar.LineUpCommand}" IsEnabled="{TemplateBinding IsMouseOver}" Style="{StaticResource ScrollBarButton}" Margin="0,0,0,-20">    
                                <Path x:Name="ArrowTop" Data="M 0,4 C0,4 0,6 0,6 0,6 3.5,2.5 3.5,2.5 3.5,2.5 7,6 7,6 7,6 7,4 7,4 7,4 3.5,0.5 3.5,0.5 3.5,0.5 0,4 0,4 z" Fill="{StaticResource ScrollBar.Static.Glyph}" Margin="3,4,3,3" Stretch="Uniform"/>    
                            </RepeatButton>-->
                                            <Track x:Name="PART_Track" IsDirectionReversed="true" IsEnabled="{TemplateBinding IsMouseOver}" Grid.Row="1">
                                                <Track.DecreaseRepeatButton>
                                                    <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Style="{StaticResource RepeatButtonTransparent}" HorizontalAlignment="Left" Width="17"/>
                                                </Track.DecreaseRepeatButton>
                                                <Track.IncreaseRepeatButton>
                                                    <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Style="{StaticResource RepeatButtonTransparent}"/>
                                                </Track.IncreaseRepeatButton>
                                                <Track.Thumb>
                                                    <Thumb Style="{StaticResource ScrollBarThumbVertical}" Margin="2"/>
                                                </Track.Thumb>
                                            </Track>
                                            <!--<RepeatButton x:Name="PART_LineDownButton" Command="{x:Static ScrollBar.LineDownCommand}" IsEnabled="{TemplateBinding IsMouseOver}" Grid.Row="2" Style="{StaticResource ScrollBarButton}"  Margin="0,-20,0,0">    
                                <Path x:Name="ArrowBottom" Data="M 0,2.5 C0,2.5 0,0.5 0,0.5 0,0.5 3.5,4 3.5,4 3.5,4 7,0.5 7,0.5 7,0.5 7,2.5 7,2.5 7,2.5 3.5,6 3.5,6 3.5,6 0,2.5 0,2.5 z" Fill="{StaticResource ScrollBar.Static.Glyph}" Margin="3,4,3,3" Stretch="Uniform"/>    
                            </RepeatButton>-->
                                        </Grid>

                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.Resources>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" Foreground="White"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.Background>
                        <SolidColorBrush Color="Transparent"/>
                    </ListBox.Background>
                    <ListBox.BorderBrush>
                        <SolidColorBrush Color="Transparent"/>
                    </ListBox.BorderBrush>
                </ListBox>
            </Border>
            <Border Background="{StaticResource TransparentBlack}" CornerRadius="0,0,20,20" Width="550">
                <Border Style="{StaticResource tbxBorder}" Width="200" Height="50" Margin="0,0,0,10">
                    <Button x:Name="addBtn" Content="Add Weapon" Width="200" Height="50"/>
                </Border>
            </Border>
        </StackPanel>


        <Border Grid.Column="2" Margin="20" Background="{StaticResource TransparentBlack}" CornerRadius="50">
            <Grid>
                <Label Content="Assault Rifle" Foreground="White" Margin="20,0" FontSize="50" FontWeight="DemiBold"/>
                <StackPanel Margin="40" >
                    <Border Background="{DynamicResource GrayToLightGrayGradient}" CornerRadius="50" Margin="0,0,0,20">
                        <StackPanel>
                            <Label Content="AK-47" Margin="40,20,0,0" FontSize="40" FontWeight="DemiBold"/>
                            <Image Height="280" Source="/12A_Projektmunka;component/img/ak-47.png" Width="700" Margin="0,10,0,50"/>
                        </StackPanel>
                    </Border>


                    <!--statok-->
                    <DockPanel>
                        <Label Content="Name:"/>
                        <Border Style="{StaticResource tbxBorder}">
                            <TextBox x:Name="nameTbx" Text="{Binding name}"/>
                        </Border>
                    </DockPanel>

                    <DockPanel Margin="0,7,0,0">
                        <Label Content="Cost:"/>
                        <Border Style="{StaticResource tbxBorder}" Height="50" Width="100" HorizontalAlignment="Right">
                            <Grid>
                                <Label Content="$" Foreground="Green"/>
                                <TextBox x:Name="costTbx" Padding="15,0,0,0" Height="50" Width="100" HorizontalAlignment="Right"/>
                            </Grid>
                        </Border>
                    </DockPanel>

                    <DockPanel Margin="0,7,0,0">
                        <Label Content="Ammo:"/>
                        <Border Style="{StaticResource tbxBorder}" Height="50" Width="100" HorizontalAlignment="Right">
                            <TextBox x:Name="ammoTbx" Height="50" Width="100" HorizontalAlignment="Right"/>
                        </Border>
                    </DockPanel>

                    <DockPanel Margin="0,7,0,0">
                        <Label Content="Damage:"/>
                        <Border Style="{StaticResource tbxBorder}" Height="50" Width="100" HorizontalAlignment="Right">
                            <TextBox x:Name="damageTbx" Height="50" Width="100" HorizontalAlignment="Right"/>
                        </Border>
                    </DockPanel>
                    <Slider x:Name="damageSldr" Minimum="0" Maximum="100" Width="800" HorizontalAlignment="Left" Margin="20, 5"/>


                    <Label Content="Fire rate:" Margin="0,7,0,0"/>
                    <Slider x:Name="frateSldr" Minimum="0" Maximum="10" Width="800" HorizontalAlignment="Left" Margin="20, 5"/>

                    <DockPanel Margin="0,7,0,0">
                        <Label Content="Penetration:"/>
                        <Border Style="{StaticResource tbxBorder}" Height="50" Width="100" HorizontalAlignment="Right">
                            <TextBox x:Name="penTbx" Height="50" Width="100"                    HorizontalAlignment="Right"/>
                        </Border>
                    </DockPanel>
                    <Slider x:Name="penSldr" Minimum="0" Maximum="100" Width="800" HorizontalAlignment="Left" Margin="20, 5"/>


                    <DockPanel x:Name="buttonsDp" Margin="0,30" HorizontalAlignment="Center">
                        <Border Style="{StaticResource tbxBorder}" Margin="0,0,50,0">
                            <Button x:Name="modifyBtn" Content="Modify" Height="50" Width="200"/>
                        </Border>
                        <Border Style="{StaticResource tbxBorder}" Margin="50,0">
                            <Button x:Name="delBtn" Content="Delete" Height="50" Width="200"/>
                        </Border>
                    </DockPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
